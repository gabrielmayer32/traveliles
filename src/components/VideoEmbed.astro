---
interface Props {
	videoSource: 'youtube' | 'vimeo' | 'facebook' | 'instagram';
	videoId: string;
	title?: string;
}

const { videoSource, videoId, title = 'Vidéo Travel-Îles' } = Astro.props;
const src = {
	youtube: `https://www.youtube.com/embed/${videoId}`,
	vimeo: `https://player.vimeo.com/video/${videoId}`,
	facebook: `https://www.facebook.com/plugins/video.php?href=${encodeURIComponent(videoId)}`,
	instagram: `https://www.instagram.com/reel/${videoId}/embed/`,
}[videoSource];
---

<div class="video-embed">
	<iframe
		src={src}
		title={title}
		loading="lazy"
		allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share"
		allowfullscreen
	></iframe>
</div>

<style>
	.video-embed {
		aspect-ratio: 16 / 9;
		width: 100%;
		background: var(--navy-950);
	}

	iframe {
		display: block;
		width: 100%;
		height: 100%;
		border: 0;
	}
</style>
